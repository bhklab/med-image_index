# Local setuptools/hatch output when developing the package
*.egg-info/

# pixi environments
.pixi/
!.pixi/config.toml

# data lives on HF Hub, not in this repo
indexed_datasets/
indexed_datasets2/
indexed_datasets.tar.gz
temp_data/

.env

parquet_index
new_index
